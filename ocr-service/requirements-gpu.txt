# ---------------------------------------------------------------------------
# GPU / production extras — install on the A100 host on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-gpu.txt
#
# Pick the CUDA build of torch that matches your driver (see pytorch.org).
# ---------------------------------------------------------------------------

# Qwen2.5-VL via HuggingFace transformers (local, device_map=auto fallback)
torch>=2.3
transformers>=4.49          # Qwen2.5-VL support
accelerate>=0.30
qwen-vl-utils>=0.0.8

# High-throughput batched serving across multiple GPUs (recommended default)
vllm>=0.6.3

# Optional alternative OCR backends
paddleocr>=2.7
paddlepaddle-gpu>=2.6       # or paddlepaddle (CPU)
