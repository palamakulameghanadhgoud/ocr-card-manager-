services:
  - type: web
    name: ocr-card-manager-api
    runtime: node
    region: oregon

    rootDir: server

    buildCommand: npm install
    startCommand: node src/index.js

    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false

  - type: web
    name: ocr-card-manager-web
    runtime: static

    rootDir: client

    buildCommand: npm install && npm run build
    staticPublishPath: dist

    routes:
      - type: rewrite
        source: /*
        destination: /index.html

    envVars:
      - key: VITE_API_URL
        sync: false
